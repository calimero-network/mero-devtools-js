node_modules/
dist/
pnpm-lock.yaml

# Emitted in the generator's own style; prettier owning this would fight the
# "generated example is in sync" check on every run.
codegen-example/src/generated/

# Test fixtures mirror core's own manifest shape; reformatting them produces
# diffs unrelated to the code under test.
abi-codegen/__fixtures__/

# Hand-maintained; pasted release notes use their own bullet/indent style, so
# reformatting churns on every release.
abi-codegen/CHANGELOG.md
