/** @generated by @calimero/abi-codegen — DO NOT EDIT. */

import {
  MeroJs,
} from '@calimero-network/mero-react';

// Generated types

export type FolderId = string & { readonly __brand: 'FolderId' };
export const FolderId = (value: string): FolderId => value as FolderId;

export type ContextId = string & { readonly __brand: 'ContextId' };
export const ContextId = (value: string): ContextId => value as ContextId;

export type Height = number & { readonly __brand: 'Height' };
export const Height = (value: number): Height => value as Height;

export type Ratio = number & { readonly __brand: 'Ratio' };
export const Ratio = (value: number): Ratio => value as Ratio;

export type Flag = boolean;

export type Hash32 = CalimeroBytes;

export type Tags = string[];

export type Index = Record<string, string>;

export type AliasOfFolderId = string & { readonly __brand: 'AliasOfFolderId' };
export const AliasOfFolderId = (value: string): AliasOfFolderId => value as AliasOfFolderId;

export type Role = 'Viewer' | 'Editor';

export type AliasOfRecord = FolderEntry;

export type ActionPayload =
  | { name: 'Noop' }
  | { name: 'Rename'; payload: string }

export const Action = {
  Noop: (): ActionPayload => ({ name: 'Noop' }),
  Rename: (rename: string): ActionPayload => ({ name: 'Rename', payload: rename }),
} as const;

export interface FolderEntry {
  id: FolderId;
  member: string;
  height: Height;
  role: Role;
}

export type Sha256Hex = string & { readonly __brand: 'Sha256Hex' };
export const Sha256Hex = (value: string): Sha256Hex => {
  if (!new RegExp("^[0-9a-f]{64}$").test(value)) {
    throw new TypeError("Sha256Hex must match ^[0-9a-f]{64}$");
  }
  return value as Sha256Hex;
};

export type RoutePath = string & { readonly __brand: 'RoutePath' };
export const RoutePath = (value: string): RoutePath => {
  if (!new RegExp("^/[a-z]+(/[a-z]+)*$").test(value)) {
    throw new TypeError("RoutePath must match ^/[a-z]+(/[a-z]+)*$");
  }
  return value as RoutePath;
};

export type OddHeight = number & { readonly __brand: 'OddHeight' };
export const OddHeight = (value: number): OddHeight => value as OddHeight;


/**
 * Utility class for handling byte conversions in Calimero
 */
export class CalimeroBytes {
  private data: Uint8Array;

  constructor(input: string | number[] | Uint8Array) {
    if (typeof input === "string") {
      // Hex string
      this.data = new Uint8Array(
        input.match(/.{1,2}/g)?.map((byte) => parseInt(byte, 16)) || []
      );
    } else if (Array.isArray(input)) {
      // Number array
      this.data = new Uint8Array(input);
    } else {
      // Uint8Array
      this.data = input;
    }
  }

  toArray(): number[] {
    return Array.from(this.data);
  }

  toUint8Array(): Uint8Array {
    return this.data;
  }

  static fromHex(hex: string): CalimeroBytes {
    return new CalimeroBytes(hex);
  }

  static fromArray(arr: number[]): CalimeroBytes {
    return new CalimeroBytes(arr);
  }

  static fromUint8Array(bytes: Uint8Array): CalimeroBytes {
    return new CalimeroBytes(bytes);
  }
}

/**
 * Convert CalimeroBytes instances to arrays for WASM compatibility
 */
function convertCalimeroBytesForWasm(obj: any): any {
  if (obj === null || obj === undefined) {
    return obj;
  }

  if (obj instanceof CalimeroBytes) {
    return obj.toArray();
  }

  if (Array.isArray(obj)) {
    return obj.map(item => convertCalimeroBytesForWasm(item));
  }

  if (typeof obj === "object") {
    const result: any = {};
    for (const [key, value] of Object.entries(obj)) {
      result[key] = convertCalimeroBytesForWasm(value);
    }
    return result;
  }

  return obj;
}

export class NewtypesAbiClient {
  private _mero: MeroJs;
  private _contextId: string;

  constructor(mero: MeroJs, contextId: string) {
    this._mero = mero;
    this._contextId = contextId;
  }

  /**
   * get_folder
   */
  public async getFolder(params: { id: FolderId }): Promise<FolderEntry> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'get_folder', argsJson: params });
    return response as FolderEntry;
  }

  /**
   * bind_context
   */
  public async bindContext(params: { folder_id: FolderId; context_id: ContextId }): Promise<void> {
    await this._mero.rpc.execute({ contextId: this._contextId, method: 'bind_context', argsJson: params });
  }

  /**
   * current_height
   */
  public async currentHeight(): Promise<Height> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'current_height', argsJson: {} });
    return response as Height;
  }

  /**
   * reset_index
   */
  public async resetIndex(): Promise<void> {
    await this._mero.rpc.execute({ contextId: this._contextId, method: 'reset_index', argsJson: {} });
  }

  /**
   * dispatch_action
   */
  public async dispatchAction(params: { action: ActionPayload }): Promise<void> {
    // Convert Action variant to WASM format
    const convertedParams = { ...params } as any;
    if (convertedParams.action && typeof convertedParams.action === 'object' && 'name' in convertedParams.action) {
      if ('payload' in convertedParams.action) {
        convertedParams.action = { [convertedParams.action.name]: convertedParams.action.payload };
      } else {
        convertedParams.action = convertedParams.action.name;
      }
    }
    await this._mero.rpc.execute({ contextId: this._contextId, method: 'dispatch_action', argsJson: convertedParams });
  }

  /**
   * store_hash
   */
  public async storeHash(params: { hash: Hash32 }): Promise<void> {
    await this._mero.rpc.execute({ contextId: this._contextId, method: 'store_hash', argsJson: convertCalimeroBytesForWasm(params) });
  }

  /**
   * rename_folder
   */
  public async renameFolder(params: { id: FolderId }): Promise<void> {
    await this._mero.rpc.execute({ contextId: this._contextId, method: 'rename_folder', argsJson: params });
  }

  /**
   * tag_hash
   */
  public async tagHash(params: { hash: Hash32; label: string }): Promise<void> {
    await this._mero.rpc.execute({ contextId: this._contextId, method: 'tag_hash', argsJson: convertCalimeroBytesForWasm(params) });
  }

}