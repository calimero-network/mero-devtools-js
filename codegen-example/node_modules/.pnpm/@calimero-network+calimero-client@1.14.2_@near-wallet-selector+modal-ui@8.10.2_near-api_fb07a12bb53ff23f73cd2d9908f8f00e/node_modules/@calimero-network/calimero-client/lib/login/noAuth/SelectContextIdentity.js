import React from 'react';
import ListItem from './ListItem';
import { ContextModal, ContextTitle, ContextWrapper, ContextSubtitle, AppIdContainer, ContextListContainer, NoContextWrapper, NoContextMessage, Button, } from '../Components';
export const SelectContextIdentity = ({ contextIdentities, selectedContextId, onSelectIdentity, backStep, }) => {
    const truncateText = (text) => {
        return `${text.substring(0, 4)}...${text.substring(text.length - 4, text.length)}`;
    };
    return (React.createElement(ContextModal, null,
        React.createElement(ContextTitle, null, "Select Context Identity"),
        React.createElement(ContextWrapper, null,
            React.createElement(ContextSubtitle, { separator: true, color: "#fff" },
                React.createElement("span", null, "Currently selected:")),
            React.createElement(ContextSubtitle, null,
                React.createElement("span", null, "Context ID"),
                React.createElement(AppIdContainer, null,
                    React.createElement("span", null, truncateText(selectedContextId))))),
        React.createElement(ContextWrapper, null,
            React.createElement(ContextSubtitle, { color: "#fff" },
                React.createElement("span", null, "Select an identity to login:")),
            contextIdentities.length > 0 ? (React.createElement(ContextListContainer, null, contextIdentities.map((identity, i) => (React.createElement(ListItem, { key: i, item: identity, id: i, count: contextIdentities.length, onRowItemClick: () => onSelectIdentity(selectedContextId, identity) }))))) : (React.createElement(NoContextWrapper, null,
                React.createElement(NoContextMessage, null, "No identities found")))),
        React.createElement(ContextWrapper, null, backStep && (React.createElement(Button, { onClick: backStep }, "Back to context selection")))));
};
