import React from 'react';
import { ContextModal, ContextWrapper, ContextSubtitle, ContextListContainer, NoContextWrapper, NoContextMessage, Button, } from '../Components';
import ListItem from './ListItem';
export const SelectContext = ({ contextList, setSelectedContextId, backStep, }) => {
    const handleContextSelection = (selectedContextId) => {
        setSelectedContextId(selectedContextId);
    };
    return (React.createElement(ContextModal, null,
        React.createElement(ContextWrapper, null,
            React.createElement(ContextSubtitle, { color: "#fff" },
                React.createElement("span", null, "Select a context:")),
            contextList.length > 0 ? (React.createElement(React.Fragment, null,
                React.createElement(ContextListContainer, null, contextList.map((context, i) => (React.createElement(ListItem, { key: context.id, item: context.id, id: i, count: contextList.length, onRowItemClick: handleContextSelection })))),
                backStep && React.createElement(Button, { onClick: backStep }, "Back"))) : (React.createElement(NoContextWrapper, null,
                React.createElement(NoContextMessage, null, "No contexts found"))))));
};
