import { NodeEvent } from '../subscriptions';
type WsCallback = (event: NodeEvent) => void;
export declare class ExperimentalWebSocket {
    private ws;
    private url;
    private reconnectTimeout;
    private callbacks;
    private requestId;
    constructor(baseUrl: string);
    private buildUrl;
    private connect;
    private decodeEventData;
    private scheduleReconnect;
    private clearReconnect;
    subscribe(contextIds: string[], callback: WsCallback): void;
    unsubscribe(contextIds: string[]): void;
    close(): void;
}
export {};
//# sourceMappingURL=websocket.d.ts.map