import { RpcClient, RpcQueryResponse, RpcQueryParams, RequestConfig, RpcResult } from '../types/rpc';
import { HttpClient } from '../api/httpClient';
/**
 * @class JsonRpcClient
 * @description A client for the JSON RPC.
 */
export declare class JsonRpcClient implements RpcClient {
    private readonly path;
    private httpClient;
    constructor(httpClient: HttpClient);
    private handleRpcError;
    /**
     * @function execute
     * @description Executes a JSON RPC request - query or mutate.
     * @param {RpcQueryParams<Args>} params - The parameters for the JSON RPC request.
     * @param {RequestConfig} config - The configuration for the JSON RPC request.
     * @returns {Promise<RpcResult<RpcQueryResponse<Output>>>} The result of the JSON RPC request.
     */
    execute<Args, Output>(params: RpcQueryParams<Args>, config?: RequestConfig): Promise<RpcResult<RpcQueryResponse<Output>>>;
    request<Params, Result>(method: string, params: Params, config?: RequestConfig): Promise<RpcResult<Result>>;
    getRandomRequestId(): number;
}
//# sourceMappingURL=jsonrpc.d.ts.map