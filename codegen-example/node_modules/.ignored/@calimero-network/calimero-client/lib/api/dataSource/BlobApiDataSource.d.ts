import { ApiResponse } from '../../types/api-response';
import { HttpClient } from '../httpClient';
import { BlobApi, BlobUploadResponse, BlobMetadataResponse, BlobListResponseData } from '../blobApi';
export declare class BlobApiDataSource implements BlobApi {
    private client;
    constructor(client: HttpClient);
    private get baseUrl();
    uploadBlob(file: File, onProgress?: (progress: number) => void, expectedHash?: string): ApiResponse<BlobUploadResponse>;
    downloadBlob(blobId: string, contextId: string): Promise<Blob>;
    getBlobMetadata(blobId: string): ApiResponse<BlobMetadataResponse>;
    listBlobs(): ApiResponse<BlobListResponseData>;
    deleteBlob(blobId: string): ApiResponse<void>;
}
//# sourceMappingURL=BlobApiDataSource.d.ts.map