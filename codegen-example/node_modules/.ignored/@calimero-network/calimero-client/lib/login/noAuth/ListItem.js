import React from 'react';
import styled from 'styled-components';
const RowItem = styled.div `
  display: flex;
  width: 100%;
  align-items: center;
  gap: 1px;
  font-size: 0.875rem;
  text-align: left;
  padding-right: 1.5rem;
  border-top: 1px solid #23262d;
  ${(props) => props.$hasBorders && `border-bottom: 1px solid #23262D;`}

  .row-item {
    padding: 0.75rem 0rem;
    display: flex;
    align-items: center;
    width: 25%;
  }
  .name {
    text-align: left;
    &:hover {
      color: #4cfafc;
      cursor: pointer;
    }
  }
`;
export default function ListItem({ item, id, count, onRowItemClick, }) {
    return (React.createElement(RowItem, { key: id, "$hasBorders": id === count },
        React.createElement("div", { className: "row-item name", onClick: () => onRowItemClick && onRowItemClick(item) }, item)));
}
