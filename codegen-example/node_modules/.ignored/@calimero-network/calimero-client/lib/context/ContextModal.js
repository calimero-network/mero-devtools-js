import React, { useState } from 'react';
import { JoinContext } from './JoinContext';
import { InviteContext } from './InviteContext';
import { CreateContext } from './CreateContext';
import { Identity } from './Identity';
import { ContextModalWrapper, TabButton, TabContent, Tabs } from './Components';
const contextActions = [
    {
        label: 'Join Context',
        id: 'join',
    },
    {
        label: 'Invite to Context',
        id: 'invite',
    },
    {
        label: 'Create Context',
        id: 'create',
    },
    {
        label: 'Identity',
        id: 'identity',
    },
];
export const ContextModal = () => {
    const [activeTab, setActiveTab] = useState('join');
    return (React.createElement(ContextModalWrapper, null,
        React.createElement(Tabs, null, contextActions.map((item, i) => (React.createElement(TabButton, { key: i, onClick: () => setActiveTab(item.id), className: activeTab === item.id ? 'active' : '' }, item.label)))),
        React.createElement(TabContent, null,
            activeTab === 'join' && React.createElement(JoinContext, null),
            activeTab === 'invite' && React.createElement(InviteContext, null),
            activeTab === 'create' && React.createElement(CreateContext, null),
            activeTab === 'identity' && React.createElement(Identity, null))));
};
